# Deployed QA backend — no trailing slash. Not secret.
VITE_API_BASE_URL=https://floorguide-qa-endpoint.fly.dev

# Set to "true" to use isolated local mock data instead of the live API.
VITE_USE_MOCK_API=false
